Update the frontend API URL to use the live backend and push the changes to GitHub.

## Steps:

1. Open the frontend API file and replace:
   http://127.0.0.1:8000/api

with:
https://ai-chat-agent-i4zq.onrender.com/api

2. Save the changes.

3. Run the following Git commands:

git add .
git commit -m "Update API URL for production"
git push

## Goal:

Ensure the frontend connects to the live backend and the changes are successfully pushed to GitHub.
